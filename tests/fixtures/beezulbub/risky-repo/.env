# FIXTURE ONLY — fake values. This file exists so the Beezulbub poison filter
# tests can detect a committed .env file. Nothing here is a real credential.
API_KEY=fake-fixture-value
DATABASE_URL=postgres://fixture:fixture@localhost:5432/fixture
