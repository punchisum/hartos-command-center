# Trigger.dev Runbook

Phase 4 includes the enqueue boundary in `src/trigger/enqueue.ts` and a provider check script.

Required env:

- `TRIGGER_SECRET_KEY`

Run:

```bash
npm run verify:trigger
```

Actual Trigger.dev task registration and enqueue implementation are Phase 5 work.
